# Installation

## From PyPI

```bash
pip install webgenai
```

## From Source

```bash
git clone https://github.com/Lumi-node/webgenai.git
cd webgenai
pip install -e ".[dev]"
```

## Requirements

- Python >= 3.10
- Dependencies: , PIL, ane_design_model, importlib, numpy, tempfile, torch
